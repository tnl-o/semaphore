# 🔍 Отчёт о проверке безопасности и работоспособности Semaphore UI (Rust)

**Дата проверки**: 2026-02-28
**Проверку выполнил**: Автоматизированный аудит
**Статус**: ⚠️ Требуются исправления

---

## 📋 Резюме

Проведён комплексный аудит проекта Semaphore UI (Rust-версия):
- ✅ Проверка уязвимостей зависимостей (osv-scanner, cargo-audit)
- ✅ Анализ безопасности кода
- ✅ Проверка работоспособности (компиляция)
- ✅ Анализ документации

---

## 🔒 Безопасность зависимостей

### Rust зависимости (cargo-audit + osv-scanner)

#### 🔴 Критические уязвимости (1)

| Уязвимость | Серьёзность | Пакет | Решение |
|------------|-------------|-------|---------|
| **RUSTSEC-2023-0071** (Marvin Attack) | 5.9 (Medium) | `rsa 0.9.10` | Нет доступного исправления |

**Детали**:
- Атака Marvin - атака по времени на RSA decryption
- Влияние: **Низкое** - RSA используется только для генерации ключей
- Митигация: Не использовать RSA decryption в критичных по времени операциях

#### 🟡 Предупреждения (2)

| Уязвимость | Пакет | Статус |
|------------|-------|---------|
| **RUSTSEC-2025-0057** | `fxhash 0.2.1` | Не поддерживается |
| **RUSTSEC-2024-0384** | `instant 0.1.13` | Не поддерживается |

**Зависимости**:
```
fxhash 0.2.1
└── sled 0.34.7 (BoltDB-подобное хранилище)

instant 0.1.13
└── parking_lot 0.11.2
    └── sled 0.34.7
```

**Рекомендации**:
- Рассмотреть миграцию с `sled` на `redb` или `rocksdb`
- Обновить `parking_lot` до версии 0.12+

### Web зависимости (npm audit + osv-scanner)

#### 🔴 Уязвимости (30+)

**Критические**:
- `cross-spawn <6.0.6` - ReDoS (7.5 High)
- `minimatch 3.x` - ReDoS (8.7 High)

**Средние**:
- `@babel/runtime <7.26.10` - RegExp complexity (6.2 Moderate)
- `ajv <8.18.0` - ReDoS при использовании `$data` (5.5 Moderate)
- `brace-expansion 1.x, 2.x` - ReDoS (3.1 Low)

**Рекомендации**:
```bash
cd web
npm audit fix
npm update
```

---

## 🛡 Безопасность кода

### Положительные аспекты ✅

1. **Хеширование паролей**:
   - Используется `bcrypt` с cost-фактором 12
   - Надёжная защита от brute-force атак

2. **JWT токены**:
   - Алгоритм HS256
   - Время жизни 24 часа
   - Секретный ключ через переменную окружения

3. **SSH ключи**:
   - Поддержка RSA, ED25519, ECDSA
   - Автоматическая установка прав 0o600
   - Поддержка passphrase

4. **Валидация входных данных**:
   - Используется `validator` crate
   - Проверка всех пользовательских данных

### Проблемные области ⚠️

1. **JWT Secret**:
   - По умолчанию используется `"dev-secret-key-change-in-production"`
   - **Требуется**: Обязательная установка через `SEMAPHORE_JWT_SECRET`

2. **RSA уязвимость**:
   - Зависимость `rsa 0.9.10` подвержена Marvin Attack
   - **Требуется**: Мониторинг обновлений

3. **Неподдерживаемые зависимости**:
   - `sled`, `fxhash`, `instant` не поддерживаются
   - **Требуется**: План миграции на альтернативы

---

## 🔧 Работоспособность проекта

### Статус компиляции

**Результат**: ⚠️ **1334 ошибки компиляции**

**Основные проблемы**:

1. **Конфликты имён** (ambiguous names):
   - Множественные glob импорты в `handlers/mod.rs`
   - Функции с одинаковыми именами в разных модулях

2. **Несоответствие типов**:
   - `max_parallel_tasks: Option<i32>` vs `i32`
   - `ProjectUserRole` vs `String`

3. **Отсутствующие импорты**:
   - `Query`, `Path` из `axum::extract`
   - `Subcommand` из `clap`

4. **Несоответствия имён функций**:
   - `create_project` vs `add_project`
   - `AuthService` vs `LocalAuthService`

### Исправленные проблемы ✅

В ходе проверки исправлено:
- ✅ Удалён дублирующийся файл `projects.rs`
- ✅ Добавлен `once_cell` в зависимости
- ✅ Исправлен тип `BackupProject.max_parallel_tasks`
- ✅ Добавлен `CliResult` тип в CLI модуль
- ✅ Исправлены импорты `LocalAuthService`
- ✅ Добавлены методы `generate_token` и `verify_token`
- ✅ Исправлены импорты `Query` и `Path` в handlers
- ✅ Добавлена функция `extract_token_from_header`
- ✅ Исправлен `version.rs` для обработки отсутствующих переменных окружения
- ✅ Удалён несуществующий импорт `BuildRepo` из git2
- ✅ Исправлены пути к типам (`TotpConfig`, `ProjectUserRole`)

### Требуемые исправления ⚠️

**Приоритетные** (блокируют компиляцию):
1. Исправить несоответствие типов в `projects/users.rs`
2. Убрать glob импорты в `handlers/mod.rs`
3. Исправить 1300+ ошибок компиляции

**Рекомендуемые**:
1. Рефакторинг импортов для избежания конфликтов имён
2. Добавление явных имён модулей при экспорте функций
3. Обновление тестов после исправления типов

---

## 📚 Документация

### Актуальная документация ✅

| Файл | Статус | Описание |
|------|--------|----------|
| `README.md` | ✅ Актуально | Основная документация проекта |
| `SECURITY.md` | ✅ Актуально | Политика безопасности |
| `API.md` | ✅ Актуально | REST API документация |
| `AUTH.md` | ✅ Актуально | Аутентификация и авторизация |
| `CONFIG.md` | ✅ Актуально | Конфигурация приложения |
| `CHANGELOG.md` | ⚠️ Требует обновления | История изменений |

### Устаревшая документация ⚠️

Следующие файлы содержат устаревшую информацию:
- `MIGRATION_*.md` - отчёты о миграции (завершена)
- `STATUS.md` - статус миграции (100% завершено)
- `FINAL_*.md` - финальные отчёты сессий

### Рекомендации по документации

1. **Обновить CHANGELOG.md**:
   - Добавить информацию об исправлениях безопасности
   - Указать известные уязвимости зависимостей

2. **Создать SECURITY_ADVISORY.md**:
   - Документировать уязвимость RUSTSEC-2023-0071
   - Указать шаги по митигации

3. **Обновить README.md**:
   - Добавить секцию "Известные проблемы"
   - Указать статус компиляции

---

## 📊 Статистика проверки

| Метрика | Значение |
|---------|----------|
| **Rust зависимостей проверено** | 402 |
| **NPM зависимостей проверено** | 1232 |
| **Найдено уязвимостей (Rust)** | 1 критическая, 2 предупреждения |
| **Найдено уязвимостей (NPM)** | 30+ |
| **Ошибок компиляции** | 1334 |
| **Предупреждений компиляции** | 251 |
| **Исправлено ошибок** | ~50 |

---

## 🎯 Рекомендации

### Краткосрочные (1-2 недели)

1. **Исправить ошибки компиляции**:
   - Приоритет: Блокирующие сборку
   - Тестирование после каждого исправления

2. **Обновить зависимости**:
   ```bash
   cd rust
   cargo update
   cd ../web
   npm audit fix
   ```

3. **Документировать уязвимости**:
   - Создать SECURITY_ADVISORY.md
   - Обновить SECURITY.md

### Среднесрочные (1-2 месяца)

1. **Замена неподдерживаемых зависимостей**:
   - `sled` → `redb` или `rocksdb`
   - `parking_lot` → 0.12+

2. **Мониторинг RSA**:
   - Подписаться на обновления `rsa` crate
   - Планировать миграцию на альтернативы

3. **Автоматизация проверок**:
   - Настроить GitHub Actions для `cargo audit`
   - Интегрировать `osv-scanner` в CI/CD

### Долгосрочные (3-6 месяцев)

1. **Рефакторинг импортов**:
   - Убрать glob импорты
   - Использовать явные имена модулей

2. **Улучшение безопасности**:
   - Принудительная установка `SEMAPHORE_JWT_SECRET`
   - Добавить refresh токены
   - Реализовать чёрный список токенов

3. **Обновление документации**:
   - Актуализировать все отчёты о миграции
   - Создать руководство по безопасности

---

## 🔧 Команды для проверки

### Проверка уязвимостей Rust
```bash
cd rust
cargo audit
osv-scanner --lockfile Cargo.lock
```

### Проверка уязвимостей NPM
```bash
cd web
npm audit
osv-scanner --lockfile package-lock.json
```

### Проверка компиляции
```bash
cd rust
cargo check
cargo build --release
```

### Запуск тестов
```bash
cd rust
cargo test
```

---

## 📞 Контакты

**Ответственный**: Alexander Vashurin
**Репозиторий**: https://github.com/alexandervashurin/semaphore
**Discord**: https://discord.gg/5R6k7hNGcH

---

**Дата следующего аудита**: 2026-03-07 (еженедельно)

**Статус**: ⚠️ **Требуются исправления**

**Последнее обновление**: 2026-02-28
